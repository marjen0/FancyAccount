import {StyleSheet} from 'react-native';

export const getStyles = (_theme: ITheme) =>
  StyleSheet.create({
    container: {
      flex: 1,
      paddingHorizontal: 12,
      paddingVertical: 8,
    },
  });
